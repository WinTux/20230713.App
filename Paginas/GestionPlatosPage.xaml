<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="_20230713.Paginas.GestionPlatosPage"
             Title="Gestión de platos">
    <ContentPage.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="0,5,0,5"/>
        </Style>
    </ContentPage.Resources>
    <VerticalStackLayout Margin="20">
        <Label Text="Acciones a realizar"/>
        <Entry Text="{Binding Path=plato.nombre}" Placeholder="Introduzca nombre de plato"/>
        <Button Text="Gardar" Clicked="OnGuardarClic"/>
        <Button Text="Eliminar" Clicked="OnEliminarClic"/>
        <Button Text="Cancelar" Clicked="OnCancelClic"/>
    </VerticalStackLayout>
</ContentPage>